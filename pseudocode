Upon start of the program, take the current keyboard layout
to be our main keyboard layout.

Upon start:
{
    record the current window and it's layout in the database
}

Upon keyboard layout change event:
{
    update the current window and it's layout in the database
}

Upon window change event:
{
    if window exists in database, apply it's layout
    if not, record it in the database
}
